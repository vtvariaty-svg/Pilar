Audite e corrija a fase atual do projeto antes de evoluir.

Projeto:
git@github.com:vtvariaty-svg/Pilar.git

Prioridade máxima:
Corrigir autenticação, autorização e separação entre super_admin, admin e cliente.

Regras de negócio:
- vtvariaty@gmail.com é o super_admin inicial.
- super_admin pode acessar /platform e /admin.
- super_admin pode criar/promover/desativar admins.
- admin pode acessar /admin.
- cliente pode acessar /cliente.
- cliente não pode acessar /admin nem /platform.
- usuário comum não pode criar users/{uid} com globalRole platform_admin.
- A fonte principal de permissão administrativa deve ser adminUsers/{uid}.

Correções obrigatórias:

1. AuthContext
Alterar isPlatformAdmin para ser baseado em adminUsers:
isPlatformAdmin = adminUserData?.role === 'super_admin' && adminUserData?.active === true

Manter isSuperAdminUser igual ou equivalente.

Remover dependência de userProfile.globalRole para liberar /platform.

2. Firestore Rules
Corrigir users/{uid}:
- create só pode criar perfil com globalRole == 'user'
- bloquear campos extras com hasOnly
- cliente não pode criar role administrativo
- update do próprio usuário só pode alterar name, phone e updatedAt

Corrigir adminUsers/{uid}:
- bootstrap só para request.auth.token.email == 'vtvariaty@gmail.com'
- exigir email == 'vtvariaty@gmail.com'
- exigir active == true
- exigir role == 'super_admin'
- bloquear campos extras com hasOnly
- apenas super_admin pode criar outros admins
- apenas super_admin pode atualizar/desativar admins

3. PlatformAdminRoute
Garantir que usa isPlatformAdmin baseado em adminUsers.role == super_admin.

4. CustomerRoute
Não tratar qualquer usuário logado como cliente.
Se usuário é admin/super_admin, redirecionar para /admin/dashboard ou /platform.
Se usuário comum/cliente, permitir /cliente.

5. AdminLeadsPage
Corrigir encoding:
- trocar “Â·” por “·”
- trocar “solicitaÃ§Ãµes” por “solicitações”
- salvar arquivo em UTF-8 sem BOM.

6. LeadDetails
Não fechar o painel ao salvar status ou observação.
Atualizar estado local ou manter drawer aberto.

7. Agendamento
Decidir uma implementação mínima:
- criar rota /agendar
ou
- no resultado da calculadora, ao clicar em “Agendar visita”, criar appointment vinculado à quoteEstimate.
Se o usuário estiver logado, salvar customerUid.
Se não estiver logado, permitir pré-agendamento público ou redirecionar para criar conta.

8. Build e lint
Rodar:
npm install
npm run build
npm run lint

Corrigir qualquer erro TypeScript ou ESLint.

9. Deploy
Após corrigir:
firebase deploy --only firestore:rules,storage,hosting

Não prosseguir para novas features se build ou rules estiverem quebrados.

Depois dessas correções, evoluir para:
- timeline única do pedido no admin
- timeline do pedido no cliente
- vínculo quoteEstimate -> appointment -> lead
- botão “converter estimativa em pedido”
- status visível ao cliente